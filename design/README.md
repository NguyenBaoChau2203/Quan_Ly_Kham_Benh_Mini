# design

Export UI mẫu từ Google Stitch.

- `stitch_mini_clinic_management_system/`: 9 màn hình (mỗi màn có `screen.png` và `code.html`)
- `clinical_precision/DESIGN.md`: token màu/chữ/khoảng cách + guideline để implement WinForms

Lưu ý:

- Đây là mockup tham khảo. Khi implement WinForms cần bám layout, density, tên label, và trạng thái bắt buộc theo `docs/Master_Plan.md`.
